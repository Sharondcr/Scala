// ask user to enter 5 Valuegroup it 2 groups vowels and consonents
object assign1 {
  def main(args: Array[String]): Unit = {
    import scala.io.StdIn
//    val vowels = "aeiou"
//
//    val Input = Array.fill(5)(StdIn.readLine("Enter a word: "))
//
//    val groupbyfirstchar = Input.groupBy { word =>
//      val first = word.head.toLower
//      if (vowels.contains(first)) "vowels" else "consonants"
//    }
//    groupbyfirstchar.foreach { case (group, values) =>
//      println(s"$group:${values.mkString(", ")}")
//    }
    
  }
}
